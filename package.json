{
  "name": "playwright_cucumber_bdd",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "test": "npm run build && cucumber-js --import ./dist/hooks/**/*.js --import ./dist/steps/**/*.js -f json:./reports/cucumber_report.json && npm run report",
    "report": "node src/utils/generateReports.js"
  },
  "devDependencies": {
    "@cucumber/cucumber": "^12.2.0",
    "@playwright/test": "^1.56.1",
    "multiple-cucumber-html-reporter": "^3.9.3",
    "open": "^10.2.0",
    "playwright": "^1.56.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  }
}
